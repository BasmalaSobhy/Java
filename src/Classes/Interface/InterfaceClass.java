package Classes.Interface;

public interface InterfaceClass {

    /* Interface
        >> used to provide a set of methods that a class must implement
            >> Any class that implements an interface must implement all of its methods,
                or it must declare itself as an abstract class.

            public interface Product {

            }

        Note: you can't create an object of the interface class


        >> An interface is similar to an abstract class,
                >> except that in an abstract class there can be some methods that are implemented.

        >> All the methods of an interface are abstract methods
                >> So, there's no need to declare the methods as abstract. It's just a given.

           public void name(); >> no body for the abstract methods

     */

    public void name();


}
